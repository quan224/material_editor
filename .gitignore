# === 构建产物（build* 覆盖 build、build-mingw、build-msvc 等所有 preset 产物目录）===
build*/
out/
cmake-build-*/

# === CMake 临时（万一漏到 build 外）===
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
*.cmake
!CMakeLists.txt
!CMakePresets.json

# === 编译中间产物（万一漏到 build 外）===
*.obj
*.o
*.pdb
*.ilk
*.exp
*.lib
*.dll
*.exe

# === Qt 自动生成 ===
moc_*.cpp
ui_*.h
qrc_*.cpp
*.qmlc

# === 日志 ===
*.log
debug_log.txt

# === 系统垃圾文件 ===
Thumbs.db
desktop.ini
.DS_Store

# === UE5 官方源码（体积过大，仅作本地参考，不入库）===
UE5/
